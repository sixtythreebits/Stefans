@model IEnumerable<ProductTestimonial>

@section CSS {
    @Html.DevExpress().GetStyleSheets(new StyleSheet { ExtensionSuite = ExtensionSuite.GridView })
}

@section JS {
    @Html.DevExpress().GetScripts(new Script { ExtensionSuite = ExtensionSuite.GridView })
}

<div class="row">
    <div class="col-lg-12">
        <h1> Product Testimonnials</h1>

        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("CreateTestimonial", "Product")"> New Testimonial </a>
            </li>
        </ol>
    </div>
</div>

@Html.Action("TestimonialGrid")